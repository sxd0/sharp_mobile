# WaterBalanceController

Свободная тема: контроллер водного баланса.

## Возможности

- дневная цель воды;
- быстрые кнопки добавления порций;
- добавление своей порции;
- прогресс за день;
- бутылка воды, которая заполняется по проценту дневной цели;
- история сегодняшних записей;
- сброс статистики за сегодня;
- сохранение данных в пользовательский JSON-файл;
- справка по `F1`;
- простой CLI-режим.

## Запуск

```bash
dotnet run
```

либо

```bash
dotnet run --project WaterBalanceController
```

CLI-примеры:

```bash
dotnet run -- --add 250
dotnet run -- --status
```
